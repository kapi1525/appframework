{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "launch",
            "type": "cppdbg",
            "request": "launch",
            "preLaunchTask": "build",
            "windows": {
                "program": "${workspaceFolder}/${config:mesonbuild.buildFolder}/sandbox.exe"
            },
            "program": "${workspaceFolder}/${config:mesonbuild.buildFolder}/sandbox",
            "args": [],
            "environment": [],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "externalConsole": false,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ]
        }
    ]
}